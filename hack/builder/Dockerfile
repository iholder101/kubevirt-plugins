# Pinned by digest so the cached builder image (keyed on this Dockerfile's
# hash in hack/dockerized) rebuilds when the base image is bumped. Update
# both the tag and the digest together.
FROM golang:1.23@sha256:60deed95d3888cc5e4d9ff8a10c54e5edc008c6ae3fba6187be6fb592e19e8c0

WORKDIR /workspace
